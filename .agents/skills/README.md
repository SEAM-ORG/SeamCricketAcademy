# Project skills

Put **product-specific** skills here (`name/SKILL.md`).

Lifecycle workflows (`/boot`, `/start`, `/agent-os-bootstrap`, …) live in `../commands/` only — do not duplicate them as skills.

Craft pack: `../vendor/agent-skills` (submodule), discovered via `.opencode/skills` symlink / global OpenCode config (no project `opencode.jsonc`).
