# Agent Workflow

> **Stub — not a second OS.** Session Start/End, Local vs GitOps, verification, and roles live in root **`AGENTS.md`**.

Operational entry: skill **`session-start`** / **`session-end`**.  
Methodology: **`using-agent-skills`** (addyosmani/agent-skills under `~/.agents/skills`).  
Machine map: `~/.agents/README.md`.

Do not duplicate checklists here.
